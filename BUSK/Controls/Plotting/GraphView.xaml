<UserControl x:Class="BUSK.Controls.Plotting.GraphView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:plot="clr-namespace:BUSK.Controls.Plotting"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" MinHeight="100" MinWidth="200">
    <Grid>
        <Grid x:FieldModifier="private" x:Name="FallbackContent" Visibility="Collapsed">
            <Rectangle Stroke="{DynamicResource SystemControlBackgroundBaseMediumBrush}" RadiusX="4" RadiusY="4" StrokeDashArray="2" StrokeThickness="2"/>
            <TextBlock Style="{DynamicResource CaptionTextBlockStyle}" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Close the Mini View to show the graph here"/>
        </Grid>
        <Border x:FieldModifier="private" x:Name="GraphContentHost">
            <Grid x:FieldModifier="private" x:Name="GraphContent">
                <plot:PerformanceChart x:Name="Chart" FontSize="{StaticResource ControlContentThemeFontSize}"/>
                <Border x:FieldModifier="private" x:Name="AdditionalContentHost" Visibility="Collapsed"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
